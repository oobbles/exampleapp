<%= form_for(@friends_list) do |f| %>
  <% if @friends_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friends_list.errors.count, "error") %> prohibited this friends_list from being saved:</h2>

      <ul>
      <% @friends_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
